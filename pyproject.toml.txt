# pyproject.toml
# TODO: update documentaion link and toml specifications
[build-system]
requires      = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "EasyGCPz"
version = "0.1.0"
description = "Plug an play automation and usage of GCP databases from Python"
readme = "README.md"
authors = [{name = "Mitchell Williams"}]
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
keywords = ["GCP", "database", "automation"]
dependencies = ["google-cloud-bigquery"]
requires-python = ">=3.8"

[project.optional-dependencies]
full = ["numpy", "pandas"]

[project.urls]
Homepage = "https://github.com/mw-os/EasyGCPz"
Documentation = "https://example.com"

[project.scripts]
realpython = "easygcp.__main__:main"


